import { ArrowUpRight, ShieldAlert } from 'lucide-react'
import { PageIntro, PageShell } from '@/components/site-header'

const resources = [{ type: 'Crisis support', title: '988 Suicide & Crisis Lifeline', desc: 'Free, confidential support for people in crisis or emotional distress.', action: 'Call or text 988', href: 'tel:988', urgent: true }, { type: 'Therapy', title: 'Find a therapist', desc: 'A searchable directory to find licensed mental health professionals.', action: 'Visit Psychology Today', href: 'https://www.psychologytoday.com/us/therapists' }, { type: 'Peer support', title: '7 Cups', desc: 'Talk to a trained listener anonymously, any time you need it.', action: 'Explore 7 Cups', href: 'https://www.7cups.com' }]

export default function ResourcesPage() {
  return <PageShell><PageIntro number="04 / Resources" title={<>You deserve<br /><span className="text-[#8fe3cf]">real support.</span></>}><p>Curated places to find professional help, peer support, or immediate crisis care. If you are in immediate danger, call local emergency services.</p></PageIntro><section className="mx-auto max-w-[1240px] px-6 py-20 lg:px-10"><div className="grid gap-4 md:grid-cols-3">{resources.map((resource) => <article key={resource.title} className={`flex min-h-72 flex-col justify-between rounded-3xl border p-6 ${resource.urgent ? 'border-[#8fe3cf]/50 bg-[#252b22]' : 'border-white/10 bg-[#202321]'}`}><div><div className="mb-6 flex items-center justify-between"><span className="text-xs font-bold uppercase tracking-[0.15em] text-[#8fe3cf]">{resource.type}</span>{resource.urgent && <ShieldAlert size={18} className="text-[#8fe3cf]" />}</div><h2 className="text-xl font-bold">{resource.title}</h2><p className="mt-3 text-sm leading-relaxed text-[#999b94]">{resource.desc}</p></div><a href={resource.href} target={resource.href.startsWith('http') ? '_blank' : undefined} rel={resource.href.startsWith('http') ? 'noreferrer' : undefined} className="mt-8 flex items-center justify-between text-sm font-bold">{resource.action}<ArrowUpRight size={17} /></a></article>)}</div></section></PageShell>
}
